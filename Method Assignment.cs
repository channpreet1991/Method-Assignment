using System;

namespace C_Sharp_Blog
public class RefLocal
//Ref Method
public static ref double ref local (double (] m) f
return ref m[1);
//main()
static void Main(string[] args)
I
//Array{2] of double
double() x = (2.1, 3.4) ;
//Let's print the contents
2.1.
3.4
Console.WriteLine("InThe values are:") ;
foreach (double a in x) ( Console.WriteLine (a) ; |
/Ref Local- By reference,
to the second index
ref var i = ref ref local (x);
//Let's change the value at this reference
//This will change the value at xt]!
i=1.1:
/It is now 2/1
1.1
Console.WriteLine ("InThe values are now: ");
foreach (double a in x) { Console.WriteLine (a) ; }
}
}
}